# Default values for altinity-mcp.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# -- Number of replicas to deploy
replicaCount: 1

image:
  # -- Container image repository
  repository: ghcr.io/altinity/altinity-mcp
  # -- Container image pull policy
  pullPolicy: IfNotPresent
  # -- Overrides the image tag whose default is the chart appVersion.
  tag: ""

# -- Pull secrets for private images
imagePullSecrets: []
# -- Override the name of the chart
nameOverride: ""
# -- Override the full name of the chart
fullnameOverride: ""

serviceAccount:
  # -- Specifies whether a service account should be created
  create: true
  # -- Annotations to add to the service account
  annotations: {}
  # -- The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

# -- Additional selector labels to add to deployment matchLabels and service selector.
# WARNING: Changing these after initial install requires recreating the Deployment
# because spec.selector.matchLabels is immutable.
selectorLabels: {}
#   app: my-app
#   component: mcp

# -- Pod annotations
podAnnotations: {}

# -- Pod security context
podSecurityContext: {}
  # fsGroup: 2000

# -- Container security context
securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

service:
  # -- Service type
  type: ClusterIP
  # -- Service port
  port: 8080
  # -- Service annotations
  annotations: {}
  # -- Session affinity type. Set to "ClientIP" to enable sticky sessions.
  # sessionAffinity: ClientIP
  # -- Session affinity configuration (only used when sessionAffinity is set)
  # sessionAffinityConfig:
  #   clientIP:
  #     timeoutSeconds: 10800

# -- Prometheus metrics and Prometheus Operator discovery.
metrics:
  # -- Expose Prometheus metrics at /metrics on the HTTP service port.
  enabled: false
  serviceMonitor:
    # -- Create a ServiceMonitor. Requires the Prometheus Operator CRDs.
    enabled: false
    # -- Additional labels for selecting this ServiceMonitor.
    labels: {}
    interval: 30s
    scrapeTimeout: 10s

ingress:
  # -- Enable ingress controller resource
  enabled: false
  # -- Ingress class name
  className: ""
  # -- Ingress annotations
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    # -- Ingress host
    - host: chart-example.local
      paths:
        # -- Ingress path
        - path: /
          # -- Ingress path type
          pathType: Prefix
  # -- Ingress TLS configuration
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

# -- Probe configuration
probes:
  liveness:
    # -- Path for the liveness probe
    path: /livez
    # -- Initial delay before liveness probe starts
    initialDelaySeconds: 7
    # -- How often to perform the liveness probe
    periodSeconds: 30
  readiness:
    # -- Path for the readiness probe
    path: /health
    # -- Initial delay before readiness probe starts
    initialDelaySeconds: 7
    # -- How often to perform the readiness probe
    periodSeconds: 30

# -- Container resource requests and limits
resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

autoscaling:
  # -- Enable autoscaling
  enabled: false
  # -- Minimum number of replicas
  minReplicas: 1
  # -- Maximum number of replicas
  maxReplicas: 100
  # -- Target CPU utilization percentage
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

# -- Node labels for pod assignment
nodeSelector: {}

# -- Toleration labels for pod assignment
tolerations: []

# -- Affinity settings for pod assignment
affinity: {}

# -- Environment variables for the main container (supports `value`, `valueFrom`, etc.).
# The application reads `CLICKHOUSE_PASSWORD` from the environment and it overrides the config file value.
# Example (Kubernetes `secretKeyRef`):
# env:
#   - name: CLICKHOUSE_PASSWORD
#     valueFrom:
#       secretKeyRef:
#         name: my-secret
#         key: password
env: []

# Configuration for the altinity-mcp application
config:
  clickhouse:
    # -- ClickHouse host. In multi-cluster mode (config.multicluster.enabled),
    # the literal `{cluster}` placeholder is substituted with the validated
    # {cluster} path value from /mcp/{cluster}. Typical k8s shape:
    #   host: "chi-{cluster}-{cluster}-0-0.demo"
    # Single-cluster: use the static hostname directly.
    host: "localhost"
    # -- Optional static hostname or IP used only to open the TCP connection.
    # HTTP Host, TLS SNI, and certificate verification continue to use `host`.
    # In multi-cluster mode this value is not subject to {cluster} substitution.
    # When set, it takes precedence over HTTP_PROXY/HTTPS_PROXY for ClickHouse.
    connect_host: ""
    port: 8123
    database: "default"
    username: "default"
    password: ""
    protocol: "http"
    read_only: false
    max_execution_time: 30
    # Server-side result cap (PR #125). Replaces the deprecated `limit`
    # SQL-rewrite knob. 0 = use the runtime default.
    max_result_rows: 1000
    tls:
      enabled: false
      ca_cert: ""
      client_cert: ""
      client_key: ""
      insecure_skip_verify: false
  
  server:
    transport: "http"
    address: "0.0.0.0"
    port: 8080
    tls:
      enabled: false
      cert_file: ""
      key_file: ""
      ca_cert: ""
    jwe:
      enabled: false
      jwt_secret_key: ""
      jwe_secret_key: ""
      token_param: "token"
    # -- OAuth 2.0 authentication configuration
    # When enabled, MCP server validates incoming OAuth tokens and can forward them to ClickHouse
    # For ClickHouse token-based authentication, use with ClickHouse Antalya build (25.8+)
    # that supports token_processors in config
    oauth:
      # -- Enable OAuth 2.0 authentication
      enabled: false
      # -- OAuth token issuer URL for validation (e.g., "https://accounts.google.com")
      issuer: ""
      # -- URL to fetch JSON Web Key Set for token validation
      # If empty, will be discovered from issuer's .well-known/openid-configuration
      jwks_url: ""
      # -- Expected audience claim in the token
      audience: ""
      # -- OAuth client ID
      client_id: ""
      # -- OAuth client secret
      client_secret: ""
      # -- OAuth token endpoint URL
      token_url: ""
      # -- OAuth authorization endpoint URL
      auth_url: ""
      # -- OAuth scopes to request
      scopes: []
      # -- Required scopes for access (token must have all of these)
      required_scopes: []
      # -- JWT claim holding a JSON array of ClickHouse role names to activate
      # per request (e.g. "https://clickhouse/roles"). When set, only the
      # role_filter-matching subset is activated via HTTP `role=` params,
      # narrowing the user's active roles. Empty disables the feature.
      role_claim: ""
      # -- Optional regex narrowing which role_claim roles are activated
      # (e.g. "^anon_"). When empty, all roles in the claim are activated (the
      # IdP curates the set; CH re-validates the token and enforces grants). An
      # empty resolved set fails closed (request denied), never falling back to
      # the full grant. Anchor it (^…/…$): it is a partial match, so an
      # unanchored "anon" would also match "not_anon_real".
      role_filter: ""
      # -- JWT claim used as the ClickHouse Basic-auth username on the
      # ch-jwt-verify sidecar path (#152). Empty (default) uses the `email`
      # claim with a namespaced `*/email` fallback. When set (e.g. "username",
      # "preferred_username", "sub"), a strict top-level lookup of that single
      # claim is used and a missing/empty/non-string value fails closed rather
      # than falling back to another identity. Must match the sidecar's
      # identity.username_claim. Applies only to the Basic path, not Bearer.
      username_claim: ""
    # Dynamic tools generated from ClickHouse views
    dynamic_tools: []
    #  - regexp: "db\\..*"
    #    prefix: "custom_"

  # -- Multi-cluster routing (#132). When enabled, MCP serves N clusters
  # from one process via /mcp/{cluster}. clickhouse.host must contain the
  # literal `{cluster}` placeholder (substituted at request time with the
  # validated cluster name). Incompatible with server.jwe.enabled and
  # server.openapi.enabled (deferred to v2).
  multicluster:
    # -- Enable URL-path multi-cluster routing
    enabled: false
    # -- Whitelist of cluster names accepted in /mcp/{cluster}. Must each
    # be a valid RFC 1123 DNS label (^[a-z0-9]([-a-z0-9]{0,61}[a-z0-9])?$).
    # An empty list allows any valid RFC 1123 cluster name.
    cluster_allowlist: []
    # -- Maximum number of (bearer, cluster) catalog cache entries.
    # Default 10000. Min 100.
    catalog_cache_max: 10000
    # -- Fallback TTL for catalog cache entries when the JWT carries no
    # exp claim. Allowed range: 1m .. 24h.
    catalog_ttl_fallback: 15m
    # -- TTL for denied (auth-class) catalog cache entries. Allowed range:
    # 10s .. 5m.
    catalog_negative_ttl: 60s

  logging:
    level: "info"
